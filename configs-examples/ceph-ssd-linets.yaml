include: default.yaml


explicit_nodes:
    "ssh://root@172.17.1.2::/root/.ssh/id_rsa": testnode
internal:
    var_dir_root: /tmp/perf_tests

tests:
    - io:
        cfg: hdd
        params:
            FILENAME: /dev/rbd1
            NUM_ROUNDS: 7
            TEST_FILE_SIZE: 20G